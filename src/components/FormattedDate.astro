---
const { date, ...props } = Astro.props;
---

<time datetime={date.toISOString()} {...props}>
  {
    date.toLocaleDateString("ko-kr", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
